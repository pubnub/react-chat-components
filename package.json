{
  "name": "pubnub-chat-components-monorepo",
  "version": "1.0.0",
  "description": "This monorepository uses npm v7 workspaces feature to crosslink dependencies between components library and sample applications.",
  "main": "index.js",
  "workspaces": [
    "lib",
    "samples"
  ],
  "scripts": {
    "start": "concurrently \"cd lib && npm run build:watch\" \"npm run start --prefix samples\"",
    "build": "cd lib && npm run build",
    "test": "jest --config lib/jest.config.js",
    "test:watch": "jest --config lib/jest.config.js --watch",
    "storybook": "start-storybook -p 6006 -c docs",
    "storybook:build": "build-storybook -c docs",
    "storybook:deploy": "npm run storybook:build && gh-pages -d storybook-static"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
