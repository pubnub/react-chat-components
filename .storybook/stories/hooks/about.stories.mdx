import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Custom Hooks/About" />

# Custom Hooks

Apart from the actual components, the Chat Components package comes with a set of custom React Hooks.
The hooks were created to easily fetch metadata stored within **PubNub Objects** and pass it directly into components.
You can skip this section if metadata is handled externally.


